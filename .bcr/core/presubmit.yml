matrix:
  platform: ["ubuntu2004", "macos", "windows"]
tasks:
  verify_targets:
    name: "Verify Core targets"
    platform: ${{ platform }}
    bazel: 7.x
    build_targets:
      - "@rules_runfile_codegen_core//..."
