matrix:
  platform: ["ubuntu2004", "macos", "windows"]
tasks:
  verify_targets:
    name: "Verify Go targets"
    platform: ${{ platform }}
    build_targets:
      - "@rules_runfile_codegen_go//:defs_bzl"
